#include "basic.h"

typedef struct Server {

} Server;

void server_init(byte *raw_x509_cert, inta raw_x509_cert_size) {

}

void server_process(Server *server, byte *raw_signed_script, inta raw_signed_script_size) {
    
}
